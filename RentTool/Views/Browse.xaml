<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="RentTool.Browse">
    <ContentPage.Content>

        <Grid Margin="10,20,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <SearchBar Placeholder="Search a tool..." Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"/>

            <Label Text="Tools" Grid.Row="2" Grid.Column="0" Margin="0,10,0,0" FontAttributes="Bold"/>
            <Label Text="F" Grid.Row="2" Grid.Column="1" Margin="0,10,5,0"/>
            <Label Text="D" Grid.Row="2" Grid.Column="2" Margin="0,10,0,0"/>

            <!--<ListView ItemsSource="{Binding Items}"
              CachingStrategy="RecycleElement"
              HasUnevenRows="True" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,5,0">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>

                                <Label Text="{Binding toolID}"
                               FontAttributes="Bold" />
                                <Label Text="{Binding image}" />
                                <Label Text="{Binding pricePerDay}" />
                                <Label Text="{Binding distance}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->

            <CollectionView ItemsSource="{Binding Items}" ItemsLayout="VerticalGrid, 2" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Image
                    Grid.Row="0"
               Source="{Binding image}"
                       Aspect="Fill"
                       HeightRequest="90"
                       WidthRequest="90" />
                            <Label Grid.Row="1" Grid.Column="0"
                       Text="{Binding name}"
                       FontAttributes="Bold" />
                            <Label Grid.Row="1" Grid.Column="1"
                       Text="{Binding distance}"
                       FontAttributes="Italic" FontSize="12" VerticalOptions="Center" />
                            <Label Grid.Row="2"
                       Grid.Column="0"
                       Text="{Binding pricePerDay, StringFormat='${0:N}'}"      
                       VerticalOptions="End" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </Grid>
    </ContentPage.Content>
</ContentPage>
